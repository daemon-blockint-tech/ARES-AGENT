{
  "name": "ares-agent-ts",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript LangGraph agent layer for ARES-AGENT Solana audit platform",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "start": "node dist/cli.js",
    "scan": "node dist/cli.js scan",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@langchain/core": "^1.0.0",
    "@langchain/langgraph": "^1.0.0",
    "langchain": "^1.0.0",
    "langsmith": "^0.8.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
